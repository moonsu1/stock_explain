# 백엔드 API 주소 (빌드 시점에 주입됨, VITE_ 접두사 필수)
# 로컬: 같은 PC에서 8000 포트로 백엔드 실행 시
VITE_API_URL=http://localhost:8000

# Vercel 배포 시 (브라우저에서 이 URL로 직접 호출):
# VITE_BACKEND_URL=https://your-backend.up.railway.app
# 또는 ngrok: VITE_BACKEND_URL=https://xxxx.ngrok-free.app
# 설정 후 반드시 Redeploy 필요.
#
# /api/proxy 사용 시(카톡 인앱 등): Vercel에 BACKEND_URL도 같은 값으로 설정
# BACKEND_URL=https://your-backend.up.railway.app
