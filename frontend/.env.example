# 백엔드 API 주소 (빌드 시점에 주입됨, VITE_ 접두사 필수)
# 로컬: 같은 PC에서 8000 포트로 백엔드 실행 시
VITE_API_URL=http://localhost:8000

# Vercel 배포 시: 프론트는 무조건 /api/proxy 사용. VITE_BACKEND_URL 설정하지 말고
# Vercel 프로젝트 환경 변수에 BACKEND_URL 만 설정 (백엔드 풀 URL).
# 예: BACKEND_URL=https://your-backend.up.railway.app 또는 ngrok URL
