# 백엔드 API 주소 (빌드 시점에 주입됨, VITE_ 접두사 필수)
# 로컬: 같은 PC에서 8000 포트로 백엔드 실행 시
VITE_API_URL=http://localhost:8000

# Vercel 배포 시 필수: Settings → Environment Variables 에서 설정 후 재배포
# 안 넣으면 Failed to fetch (백엔드 연결 안 됨). 자세한 건 docs/VERCEL_DEPLOY.md
# VITE_API_URL=https://your-backend.up.railway.app
