# 백엔드 API 주소 (빌드 시점에 들어감)
# - Vercel 배포: Vercel 대시보드 → Environment Variables 에서 설정
# 로컬 백엔드 (같은 PC에서 Docker 등으로 8000 포트 실행 시)
VITE_API_URL=http://localhost:8000

# Railway 등 외부 백엔드 사용 시 예시
# VITE_API_URL=https://stockexplain-production.up.railway.app
