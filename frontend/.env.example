# 백엔드 API 주소 (빌드 시점에 주입됨, VITE_ 접두사 필수)
# 로컬: 같은 PC에서 8000 포트로 백엔드 실행 시
VITE_API_URL=http://localhost:8000

# Vercel 배포 시 (둘 다 같은 값으로 설정 권장):
# - VITE_API_URL: 프론트 빌드용 + 프록시 미사용 시 직접 호출
# - BACKEND_URL: /api/proxy 경로로 들어오는 요청을 넘길 백엔드 주소 (카톡 인앱 브라우저 대응)
# VITE_API_URL=https://your-backend.up.railway.app
# BACKEND_URL=https://your-backend.up.railway.app
